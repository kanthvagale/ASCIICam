/**
 * Every `.svg` in this directory, re-exported as a React component.
 * Typed by `src/types/svg.d.ts`, transformed by react-native-svg-transformer.
 *
 * Naming: camelCase filename -> PascalCase export, always suffixed `Icon`
 * (`logo.svg` -> `LogoIcon`, `updateAppIcon.svg` -> `UpdateAppIcon`).
 * Add a line here whenever you drop a new `.svg` alongside it.
 */
export { default as LogoIcon } from "./logo.svg";
