/**
 * ViewModel for the `/` route.
 *
 * Views stay presentational — every piece of state, derived value and action a
 * screen needs is assembled here and returned as one object. Read persisted or
 * shared state from a store in `@/store`, and fetch through `@/services/api`;
 * never call `fetch` from a view.
 */
export function useHomeController() {
  return {};
}
